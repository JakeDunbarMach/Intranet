
namespace AppModel.Constants
{
    public class SectionEnum
    {
        /// <summary>
        /// Enum for system sections
        /// </summary>
        public enum Sections
        {
            ADMIN = 1, //This must be 1
            ENTITY = 2

        };
    }
}
